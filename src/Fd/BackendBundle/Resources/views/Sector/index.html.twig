{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Lista de Sector
{% endblock %}
 
{% block main_content %}

<h1>Lista de Sector</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Descripcion</th>
            <th>Abreviatura</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_sector_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.descripcion }}</td>
            <td>{{ entity.abreviatura }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_sector_show', { 'id': entity.id }) }}">
                                                    Mostrar
                                                </a>
                    </li>
                    <li>
                        <a href="{{ path('backend_sector_edit', { 'id': entity.id }) }}">
                                                    Editar
                                                </a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('backend_sector_new') }}">
            Crear nuevo registro
        </a>
    </li>
</ul>

{% endblock %}
