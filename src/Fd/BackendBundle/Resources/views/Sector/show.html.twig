{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Sector
{% endblock %}
 
{% block main_content %}

<h1>Sector</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Descripcion</th>
            <td>{{ entity.descripcion }}</td>
        </tr>
        <tr>
            <th>Abreviatura</th>
            <td>{{ entity.abreviatura }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_sector') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('backend_sector_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>
        <form action="{{ path('backend_sector_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="boton_rojo">Eliminar</button>
        </form>
    </li>
</ul>

{% endblock %}