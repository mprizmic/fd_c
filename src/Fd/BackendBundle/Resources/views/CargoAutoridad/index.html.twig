{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Lista de CargoAutoridad
{% endblock %}
 
{% block main_content %}

<h1>Lista de CargoAutoridad</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Abreviatura</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_cargo_autoridad_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nombre }}</td>
            <td>{{ entity.abreviatura }}</td>
            <td>
                        <a href="{{ path('backend_cargo_autoridad_show', { 'id': entity.id }) }}">
                                                    Mostrar
                                                </a>||
                        <a href="{{ path('backend_cargo_autoridad_edit', { 'id': entity.id }) }}">
                                                    Editar
                                                </a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('backend_cargo_autoridad_new') }}">
            Crear nuevo registro
        </a>
    </li>
</ul>

{% endblock %}
