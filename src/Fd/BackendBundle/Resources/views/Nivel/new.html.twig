{% extends 'BackendBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - Crear Nivel
{% endblock %}

{% block main_content %}
    {{ parent() }}
    <h1>Crear nuevo nivel de enseñanza</h1>

    <form action="{{ path('backend_nivel_create') }}" method="post" {{ form_enctype(form) }}>
        <div class="ancho_10">
            {{ form_widget(form) }}
        </div>
        <div class="bloque">
            <button class="separador" type="submit">Crear</button>
            <a class="separador" href="{{ path('backend_nivel') }}">
                Volver a la lista
            </a>
        </div>
    </form>

    <br />
    <br />
{% endblock %}
