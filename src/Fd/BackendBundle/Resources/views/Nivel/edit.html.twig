{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Editar Nivel
{% endblock %}
 
{% block main_content %}

<h1>Editar Nivel</h1>

<form action="{{ path('backend_nivel_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <div class="ancho_10">
    {{ form_widget(edit_form) }}
    </div>
    <p>
        <button type="submit">Guardar</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_nivel') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <form action="{{ path('backend_nivel_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="boton_rojo">Eliminar</button>
        </form>
    </li>
</ul>

{% endblock %}