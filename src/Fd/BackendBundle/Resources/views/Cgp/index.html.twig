{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Lista de Cgp
{% endblock %}
 
{% block main_content %}

<h1>Lista de Cgp</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Numero</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_cgp_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.numero }}</td>
            <td>

                        <a href="{{ path('backend_cgp_show', { 'id': entity.id }) }}">
                                                    Mostrar ||
                                                </a>
                        <a href="{{ path('backend_cgp_edit', { 'id': entity.id }) }}">
                                                    Editar
                                                </a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('backend_cgp_new') }}">
            Crear nuevo registro
        </a>
    </li>
</ul>

{% endblock %}
