{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Lista de Pais
{% endblock %}
 
{% block main_content %}

<h1>Lista de Pais</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Codigo</th>
            <th>Descripcion</th>
            <th>Orden</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_pais_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.codigo }}</td>
            <td>{{ entity.descripcion }}</td>
            <td>{{ entity.orden }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_pais_show', { 'id': entity.id }) }}">
                                                    Mostrar
                                                </a>
                    </li>
                    <li>
                        <a href="{{ path('backend_pais_edit', { 'id': entity.id }) }}">
                                                    Editar
                                                </a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('backend_pais_new') }}">
            Crear nuevo registro
        </a>
    </li>
</ul>

{% endblock %}
