{% extends 'BackendBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - Editar GrupoEtario
{% endblock %}

{% block main_content %}
    {{ parent() }}

    <div class="titulo">Editar un grupo etario</div>

    <form action="{{ path('backend.grupo_etario.update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <div class="ancho_10">
            {{ form_widget(edit_form) }}
        </div>
        <p class="separador">
            <button type="submit">Guardar</button>
        </p>
    </form>

    <form action="{{ path('backend.grupo_etario.delete', { 'id': entity.id }) }}" method="post">
        <div class="invisible">
            {{ form_widget(delete_form) }}
        </div>
        <button class="separador boton_rojo" type="submit" >Eliminar</button>
        <a class="separador" href="{{ path('backend.grupo_etario') }}">Volver a la lista</a>
        <a class="separador" href="{{ path('backend.grupo_etario.new') }}">Crear nuevo grupo etario</a>
    </form>

    <br />
{% endblock %}