{% extends 'BackendBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - Crear GrupoEtario
{% endblock %}

{% block main_content %}
    {{ parent() }}

    <div class="titulo">Crear nuevo grupo etario </div>

    <form action="{{ path('backend.grupo_etario.create') }}" method="post" {{ form_enctype(form) }}>
        <div class="ancho_10">
            {{ form_widget(form) }}
        </div>
        <div>
            <button type="submit">Crear</button>
            <a class="separador" href="{{ path('backend.grupo_etario') }}">
                Volver a la lista
            </a>
        </div>
    </form>

    <br />
{% endblock %}
