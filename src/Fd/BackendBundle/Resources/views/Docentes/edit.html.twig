{% extends 'BackendBundle::layout.html.twig' %}
 
{% block main_content %}
{{ parent() }}

<h1>Editar cantidad de docentes de cada nivel</h1>

{% render "EstablecimientoBundle:Establecimiento:tarjeta_establecimiento" with {'establecimiento_id': entity.establecimiento.id} %}

<br />
<br />

<form action="{{ path('backend_establecimiento_docentes_nivel_actualizar', {'establecimiento_id':entity.establecimiento.id} ) }}" method="post" {{ form_enctype(edit_form) }}>
    <div class="ancho_10">
        {{ form_widget(edit_form) }}
    </div>
    <div class="float_izquierda separador">
        <input type="submit" value="Guardar" />
    </div>
</form>

<br />

<a class="float_izquierda separador" href="{{ path('establecimiento_ficha', {'establecimiento_id': entity.establecimiento.id }) }}">
    Volver a la ficha
</a>

<span class="limpiar_float"></span>
<br />
<br />

{% endblock %}