{% extends 'BackendBundle::layout.html.twig' %}
 
{% block main_content %}

<h1>Editar Títulos de Carreras</h1>

<form action="{{ path('backend_titulocarrera_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <div class="ancho_10">
    {{ form_widget(edit_form) }}
        <br />
    </div>
        <div class="float_izquierda separador">
            <button type="submit">Guardar</button>
        </div>
</form>

<form action="{{ path('backend_titulocarrera_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
    <span class="float_izquierda separador">
        <input type="submit" class="boton_rojo" value="Eliminar" onsubmit="return confirm('Está seguro?')" />
    </span>
</form>

<a href="{{ path('backend_titulocarrera_buscar') }}">
    Volver
</a>

<span class="limpiar_float"></span>

<br />
<br />

{% endblock %}