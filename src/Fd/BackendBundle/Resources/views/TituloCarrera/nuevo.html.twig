{% extends 'BackendBundle::layout.html.twig' %}
 
{% block main_content %}

<h1>Crear Título de Carrera</h1>

<form action="{{ path('backend_titulocarrera_create') }}" method="post" {{ form_enctype(form) }}>
    <div class="ancho_10">
        {{ form_widget(form) }}
    </div>
    <p>
        <button type="submit">Crear</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_titulocarrera_buscar') }}">
            Volver
        </a>
    </li>
</ul>

{% endblock %}
