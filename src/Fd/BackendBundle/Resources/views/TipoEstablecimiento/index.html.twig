{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Lista de TipoEstablecimiento
{% endblock %}
 
{% block main_content %}

<h1>Lista de TipoEstablecimiento</h1>

<ul>
    <li>
        <a href="{{ path('backend_tipo_establecimiento_new') }}">
            Crear nuevo registro
        </a>
    </li>
</ul>


<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Codigo</th>
            <th>Descripcion</th>
            <th>Orden</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_tipo_establecimiento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.codigo }}</td>
            <td>{{ entity.descripcion }}</td>
            <td>{{ entity.orden }}</td>
            <td>                        <a href="{{ path('backend_tipo_establecimiento_show', { 'id': entity.id }) }}">
                                                    Mostrar
                                                </a>                        <a href="{{ path('backend_tipo_establecimiento_edit', { 'id': entity.id }) }}">
                                                    || Editar
                                                </a>            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>


{% endblock %}
