{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Crear Turno
{% endblock %}
 
{% block main_content %}

<h1>Crear registro de Turno </h1>

<form action="{{ path('backend_turno_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Crear</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_turno') }}">
            Volver a la lista
        </a>
    </li>
</ul>

{% endblock %}
