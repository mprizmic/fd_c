{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - UnidadEducativa
{% endblock %}
 
{% block main_content %}

<h1>UnidadEducativa</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Descripcion</th>
            <td>{{ entity.descripcion }}</td>
        </tr>
        <tr>
            <th>Nombre_autoridad</th>
            <td>{{ entity.nombreautoridad }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_unidad_educativa') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('backend_unidad_educativa_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>
        <form action="{{ path('backend_unidad_educativa_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="boton_rojo">Eliminar</button>
        </form>
    </li>
</ul>

{% endblock %}