{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Lista de Orientacion
{% endblock %}
 
{% block main_content %}

<h1>Lista de Orientacion</h1>

<ul>
    <li>
        <a href="{{ path('backend_orientacion_new') }}">
            Crear nuevo registro
        </a>
    </li>
</ul>


<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Titulo</th>
            <th>Duracion</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_orientacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nombre }}</td>
            <td>{{ entity.titulo }}</td>
            <td>{{ entity.duracion }}</td>
            <td>                        <a href="{{ path('backend_orientacion_show', { 'id': entity.id }) }}">
                                                    Mostrar
                                                </a>                        <a href="{{ path('backend_orientacion_edit', { 'id': entity.id }) }}">
                                                    || Editar
                                                </a>            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{% include '::includes/volver.html.twig' %}
{% endblock %}
