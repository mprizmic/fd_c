{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Localizacion
{% endblock %}
 
{% block main_content %}

<h1>Localizacion</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_localizacion') }}">
            Volver a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('backend_localizacion_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>
        <form action="{{ path('backend_localizacion_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="boton_rojo">Eliminar</button>
        </form>
    </li>
</ul>

{% endblock %}