{% extends 'BackendBundle::layout.html.twig' %}
 
{% block title %}
{{ parent() }} - Editar Autoridad
{% endblock %}
 
{% block main_content %}
{{ parent() }}

<h1>Editar Autoridad</h1>

<form action="{{ path('backend_autoridad_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <div class="ancho_10">
            {{ form_widget(edit_form) }}
        </div>
        <br />
        <div class="float_izquierda separador">
            <button type="submit">Guardar</button>
        </div>
</form>

<form action="{{ path('backend_autoridad_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
    <span class="float_izquierda separador">
        <input type="submit" class="boton_rojo" value="Eliminar" onsubmit="return confirm('Está seguro?')" />
    </span>
</form>

<a href="{{ path('backend_autoridad_buscar') }}">
    Volver
</a>

<span class="limpiar_float"></span>

<br />
<br />

{% endblock %}