{% extends '::layout.html.twig' %}

{% block main_content %}
    {{ parent() }}

    <h1 class="centrado">Avisos</h1>
    <div class="limpiar_float"></div>

    <div class="instrucciones centrado italico chica">
        En esta página se dan avisos que guían acerca del estado de los datos cargados y del desarrollo del sistema. 
    </div>

    <br />
    <br />

    {% spaceless %}
        {% set avisos_activos = "\n" %}
        {% for aviso in avisos %}
            {% if aviso.activo %}
                {% set fecha = aviso.fecha | date('d-m-Y') %}
                {% set avisos_activos = avisos_activos ~ fecha ~ ': ' ~ aviso.descripcion ~ "\n\n"%}
            {% endif %}
        {% endfor %}

        <div class="centrado">
            <textarea name="avisos" rows="25" cols="130" readonly="readonly">
                {{ avisos_activos }}
            </textarea>
        </div>
    {% endspaceless %}

    <br />
    <br />

    <h2 class="centrado"><a href="{{ path('establecimiento_damero') }}">Continuar</a></h2>

    <br />

{% endblock %}